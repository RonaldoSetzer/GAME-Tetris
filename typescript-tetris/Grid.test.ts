/*import { Grid } from "./../src/models/Grid";
import { assert } from "chai";

describe("Grid", () => {
    let grid: Grid;

    beforeEach(() => {
        grid = new Grid(4, 6);
    });

    afterEach(() => {
        grid = null;
    });

    it("Grid - Size - Col", () => {
        assert.equal(6, grid.maxCols, "column");
    });

    it("Grid - Size - Row", () => {
        assert.equal(6, grid.maxRows, "line");
    });

});
*/
